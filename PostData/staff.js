const express = require('express')
const { db } = require('./config/db')

const router = express.Router()

// Add schedule
router.post('/staff/showtimes', async (req, res) => {

})

router.post('/staff/bookings', async (req, res) => {

})



export default router